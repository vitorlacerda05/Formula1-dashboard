{
  "compilerOptions": {
    "target": "ES2020",         /* Especifique a versão do ECMAScript de destino. */
    "module": "commonjs",       /* Especifique a geração de código do módulo. */
    "rootDir": "./",            /* Especifique o diretório raiz dos arquivos de entrada. */
    "outDir": "./dist",         /* Redirecione a estrutura de saída para o diretório. */
    "esModuleInterop": true,    /* Permite interoperabilidade de importação padrão. */
    "forceConsistentCasingInFileNames": true, /* Garante nomes de arquivos consistentes. */
    "strict": true,             /* Habilite todas as verificações de tipo estritas. */
    "skipLibCheck": true,       /* Pule a verificação de tipo de todos os arquivos de declaração (.d.ts). */
    "resolveJsonModule": true,  /* Permite importar módulos .json. */
    "sourceMap": true,          /* Gera os respectivos arquivos .map. */
    "baseUrl": ".",             /* Base directory for resolving non-relative module names. */
    "paths": {
      "@types/*": ["@types/*"]  /* Path mapping for @types folder. */
    }
  },
  "include": ["src/**/*", "routes/**/*", "@types/**/*"], /* Quais arquivos o TypeScript deve compilar. */
  "exclude": ["node_modules", "dist"] /* Quais arquivos devem ser ignorados. */
}